% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/3_sens_general.R
\name{sens_general}
\alias{sens_general}
\title{Sensitivity of general functions of OLS coefficients}
\usage{
sens_general(
  Y,
  X,
  W1,
  bar_rho,
  bar_R2,
  ellipsoid = NULL,
  f,
  grad_f,
  beta_init,
  step_size = 0.01,
  max_iter = 5000,
  tol = 1e-08,
  verbose = 100
)
}
\arguments{
\item{Y}{A \eqn{N \times 1} vector.}

\item{X}{A \eqn{N \times dX} dataframe/matrix.}

\item{W1}{A \eqn{N \times d1} dataframe/matrix or \code{NULL}.}

\item{bar_rho}{A scalar in \eqn{[0, 1)}.}

\item{bar_R2}{A scalar in \eqn{[0, 1)}.}

\item{ellipsoid}{A list computed from \code{precompute_ellipsoid()} (optional).}

\item{f}{Objective function \eqn{f(\beta)} -> scalar.}

\item{grad_f}{Gradient function \eqn{\nabla f(\beta)} -> \eqn{dX \times 1} vector.}

\item{beta_init}{Initial feasible point (\eqn{dX \times 1} vector).}

\item{step_size}{Fixed step size (scalar), or "backtrack" for backtracking line search.}

\item{max_iter}{Maximum number of iterations.}

\item{tol}{Convergence tolerance on \eqn{\|\beta^{t+1} - \beta^t\|}.}

\item{verbose}{Print progress every \code{verbose} iterations (0 = silent).}
}
\value{
A list of 6 objects where:
\itemize{
\item \code{beta_lb} stores the coefficient vector beta attaining the lower bound,
\item \code{beta_ub} stores the coefficient vector beta attaining the upper bound,
\item \code{obj_lb} stores the lower bound of \eqn{f_{g}(\beta)},
\item \code{obj_ub} stores the upper bound of \eqn{f_{g}(\beta)},
\item \code{history_lb} stores the history of optimizing lower bound of \eqn{f_{g}(\beta)},
\item \code{history_ub} stores the history of optimizing upper bound of \eqn{f_{g}(\beta)}.
}
}
\description{
\code{sens_general()} calculates the extremal values for general functions of
OLS coefficients \eqn{f_\text{g}(\beta)} by projected gradient descent.
}
